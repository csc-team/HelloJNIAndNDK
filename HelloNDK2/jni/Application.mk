# как будет оптимизироватся библиотека
#APP_OPTIM := debug

# подключение stllib
#only if you platform upper then 2.2
#APP_STL :=gnustl_static
# platform 1.6
APP_STL :=stlport_static

# библиотека которую собираем
APP_MODULES := native_lab1
